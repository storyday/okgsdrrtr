apiVersion: v1
kind: Service
metadata:
  name: okgsdrrtr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "okgsdrrtr"
    port: 8080
  selector:
    app: okgsdrrtr
